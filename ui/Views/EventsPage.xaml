<Page
    x:Class="WinForgeShell.Views.EventsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel
            Grid.Row="0"
            Orientation="Horizontal"
            Spacing="8"
            Padding="12,8">

            <TextBlock
                Text="Live Events"
                Style="{ThemeResource TitleTextBlockStyle}"
                VerticalAlignment="Center"/>

            <Button
                x:Name="ClearButton"
                Content="Clear"
                Click="ClearButton_Click"
                VerticalAlignment="Center">
                <Button.Icon>
                    <FontIcon Glyph="&#xE74D;"/>
                </Button.Icon>
            </Button>
        </StackPanel>

        <!-- Full-page scrolling event log -->
        <ScrollViewer
            Grid.Row="1"
            x:Name="EventScrollViewer"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto"
            Padding="12,4">

            <ItemsControl x:Name="EventsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock
                            Text="{x:Bind}"
                            FontFamily="Consolas, Courier New, monospace"
                            FontSize="12"
                            TextWrapping="Wrap"
                            Margin="0,1"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty state shown via code-behind when list is empty -->
        <Grid Grid.Row="1" x:Name="EmptyState" Visibility="Visible">
            <TextBlock
                Text="No events received yet. Events will appear here as the backend pushes them."
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                TextWrapping="Wrap"
                TextAlignment="Center"
                Margin="32"/>
        </Grid>
    </Grid>
</Page>
