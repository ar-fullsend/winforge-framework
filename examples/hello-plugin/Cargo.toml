[package]
name = "hello-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
winforge-plugin = { path = "../../crates/winforge-plugin" }
async-trait = "0.1"
tokio = { version = "1", features = ["rt"] }
serde_json = "1"
